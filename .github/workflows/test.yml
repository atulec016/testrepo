name: test_yaml

on: 
  push:
    tags: 
      - '*'
  workflow_dispatch:
      
jobs:
  command_test:
    runs-on: ubuntu-latest
    steps:
      - name: Code check for commit and update the branch
        run: |
          echo "Tis is test YAml"
